{
  "watch": [
    "src"
  ],
  "ext": "ts",
  "ignore": [],
  "exec": "rslib build && homebridge -U ./test/hb-config -I -D",
  "signal": "SIGTERM",
  "env": {
    "NODE_OPTIONS": "--trace-warnings --openssl-legacy-provider"
  }
}